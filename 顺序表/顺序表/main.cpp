#include<iostream>
#include"list.h"

using namespace std;

int main()
{
	/*OSL* array = (OSL*)malloc(sizeof(OSL));
	os_format(array);
	os_insert(array, 1, 1);
	os_insert(array, 2, 3);
	os_insert(array, 2, 2);
	os_insert_head(array, 0);
	os_intsert_end(array, 4);
	os_print(array);
	os_del_end(array);
	os_print(array);*/
	return 0;
}